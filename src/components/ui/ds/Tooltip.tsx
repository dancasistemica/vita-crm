import React from 'react';

export const TooltipProvider = ({ Button, children }: { Button, children: React.ReactNode }) => <>{children}</>;
export const Tooltip = ({ Button, children }: { Button, children: React.ReactNode }) => <>{children}</>;
export const TooltipTrigger = ({ Button, children }: { Button, children: React.ReactNode }) => <>{children}</>;
export const TooltipContent = ({ Button, children }: { Button, children: React.ReactNode }) => null;
